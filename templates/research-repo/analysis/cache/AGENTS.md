# Analysis Cache Rules

This directory is for generated machine artifacts only.

- Put stable reusable indexes under `indexes/`.
- Put noisy raw exports and diagnostics under `noise/`.
- Do not put final reports, customer deliverables, or feature findings here.
- Cache files may be regenerated, but do not delete cache created by another
  active task unless the selected queue task explicitly requires it.
