profile_id = "target"
profile_title = "Целевая конфигурация"
profile_status = "experimental"

[target_identity]
configuration = ""
major_version = ""

# Профиль описывает проверяемые возможности целевого релиза через статические
# признаки в исходниках. Скопируйте блоки в
# analysis/functional-gaps/profiles/<profile_id>.toml и замените target на
# реальные процедуры, функции, реквизиты или свойства целевой конфигурации.

[[capabilities.document_lifecycle_state.standard_evidence]]
kind = "source_call"
target = "ИмяПроцедурыЗаписиСостояния"
context = "типовой механизм записи состояния объекта"

[[capabilities.state_driven_action.adaptation_evidence]]
kind = "source_call"
target = "ИмяПроцедурыВыполненияСценария"
context = "расширяемый сценарий или скрипт может выполнить действие"

[[capabilities.scheduled_object_processing.standard_evidence]]
kind = "source_call"
target = "ИмяРегламентнойПроцедуры"
context = "типовая фоновая или регламентная обработка объектов"

[capabilities.document_lifecycle_state.confidence_rules]
standard_supported = "medium"

[capabilities.state_driven_action.confidence_rules]
adaptation_candidate = "medium"
