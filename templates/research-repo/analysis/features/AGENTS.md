# Feature Evidence Rules

This directory stores durable feature evidence packs.

- Edit only the feature folder for the selected queue task.
- Start new packs from `_templates/`.
- Keep source claims traceable through `evidence.csv`.
- Do not move final customer deliverables here; use `outputs/`.
- Do not overwrite another feature pack to simplify a current task.
