# Migration Requirements

This directory stores the canonical `MRQ-*` graph. Requirements, links,
lineage, and ownership are separate JSONL relations. Use bounded `show`,
`context`, and `neighbors` commands instead of scanning the complete graph.
