# Parallel Research

Run manifests and the single-writer ledger are created here. Worker traces and
isolated workspaces belong under `analysis/cache/` and are ignored. Configure
the model, worker count, timeout, source profile, and Git refs in `project.toml`.
