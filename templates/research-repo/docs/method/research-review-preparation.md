# Review Preparation

`/goal Подготовь ревью` plans review work without executing it. Run
`research-review prepare --plan`, inspect the deterministic targets and queue
plan, then use `--apply` and `research-review validate`. Prepared tasks must
name exact source artifacts and expected outputs.
