---
name: 1c-autoresearch-parallel-research-goal
description: Run `/goal Параллельное исследование` with isolated readers and one writer.
---

# Parallel Research Goal

Read `AGENTS.md` and `docs/method/parallel-research-goal.md`. Resume or plan a
compatible run, use execution settings from `project.toml`, validate every
structured decision, apply through the single writer, and stop only after the
run has no unresolved units.
