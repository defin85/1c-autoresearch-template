## 1. Freeze The Retained Surface

- [x] 1.1 Derive reviewed runtime, maintenance, and forbidden-authority inventories from the canonical scaffold and record exact ownership boundaries.
- [x] 1.2 Add scoped failing source-tree and distribution-member scans for forbidden paths, modules, parser commands, routes, registrations, `CUS-*`, and subject-card schema markers while excluding non-executable OpenSpec history.
- [x] 1.3 Add normalized two-way parity checks between the verified target, scaffold, root runtime package, compiled workspace assets, and separate packaged template.
- [x] 1.4 Before deletion, build and fingerprint a `0.2.0` baseline wheel from the exact pre-cleanup commit in an isolated worktree for the `0.2.0`→`0.3.0` upgrade check.

## 2. Keep Template Maintenance Out Of Runtime

- [x] 2.1 Make `one_c_autoresearch` and `one-c-autoresearch` use the canonical generated-repository runtime and CLI without legacy parser branches and set the release/runtime version to `0.3.0`.
- [x] 2.2 Rewrite only `scripts/sync_generated_runtime.py`, `scripts/build_workspace_template.py`, `scripts/bootstrap/new_research_repo.py`, and `scripts/checks/test_template.py`, `test_doctor.py`, `test_research_repo.py` as runtime-independent maintenance; add no maintenance package or console entry point.
- [x] 2.3 Move the existing canonical-repository `checks research` delegation and regression coverage from `src/one_c_autoresearch/checks.py` into `scripts/checks/test_research_repo.py`, using `sys.executable`, explicit `cwd`/`PYTHONPATH`, no shell, and propagated exit status.
- [x] 2.4 Verify generated repositories and runtime-only installations contain no maintenance module, script, command, or repository-generation authority.

## 3. Make Synchronization Prune Stale Files

- [x] 3.1 Replace overlay promotion and preserved legacy `cli.py`/`doctor.py` behavior with fully staged validation and exact full-tree replacement of derived runtime paths.
- [x] 3.2 Make synchronization default to a content-free sorted add/change/delete preview and require its fingerprint plus explicit apply mode against unchanged inputs before replacing the scaffold, root runtime, web source, static assets, or rollback documentation.
- [x] 3.3 Validate staged content and forbidden-authority absence, then add mismatched-plan, interrupted-replacement convergence, packaging fail-closed, unexpected-extra-file, repeated-sync, customer-data rejection, and non-empty-destination tests.

## 4. Delete Legacy Contours

- [x] 4.1 Delete the `CUS` customization registry, customer-register, subject-card, detail-map, functional-gap, reverse-map, final-gate, autopilot, and old review-dashboard modules and commands.
- [x] 4.2 Delete the queue, parallel-research, research-review, manual-cleanup, clean-rebase, external-processing, standalone normalization, paged CSV reader, and compatibility-reader modules and commands while retaining canonical source routing, stable DIF identity/lineage, semantic/physical path closure, platform-safe CSV field limits, and their target-first tests.
- [x] 4.3 Delete retired routes, registrations, schemas, top-level shims, scripts, skills, tests, fixtures, and packaged assets; allow outside target parity only the six named maintenance scripts, five active root documents, `tests/test_generated_runtime_sync.py`, and `tests/test_template_release.py`.
- [x] 4.4 Remove obsolete imports and dependencies without adding compatibility adapters or speculative abstractions.

## 5. Align Documentation And Packaging

- [x] 5.1 Rewrite README, repository map, verification matrix, AGENTS routing, and short command snippets around the canonical runtime and six root maintenance scripts.
- [x] 5.2 Remove active legacy methodology and skills while preserving historical OpenSpec archives as non-executable history.
- [x] 5.3 Remove `workspace_assets/research-template.zip` from runtime package data, clear legacy release outputs, update canonical entry points and root maintenance scripts, then rebuild final `dist/` with only the `0.3.0` wheel, `0.3.0` source distribution, and separate `research-template.zip`.
- [x] 5.4 Document release `0.3.0`, last compatible version `0.2.0`, the breaking removals, supported canonical migration boundary, and non-destructive rollback.

## 6. Verify Release Readiness

- [x] 6.1 Run canonical target Python tests, frontend tests, type checking, production build, and focused dispatcher/browser acceptance.
- [x] 6.2 Run template maintenance tests, synchronization/parity tests, forbidden-authority scans, template checks, doctor checks, and the full remaining Python suite on the existing Ubuntu, Windows, and macOS CI matrix where supported.
- [x] 6.3 Install `0.3.0` cleanly and over the fingerprinted `0.2.0` baseline, both without extras and with workspace extras; verify the canonical runtime command, absence of legacy and maintenance commands/package data, root maintenance scripts, workspace health, and compiled application shell.
- [x] 6.4 Generate a fresh repository and verify canonical doctor, Python tests, web tests/build, exact workflow catalog, and absence of legacy authorities.
- [x] 6.5 Run strict OpenSpec validation, `git diff --check`, distribution scans, and a final source review proving no customer data or unrelated worktree changes were included.

## Разрывы ревью

- [x] R1 Automate the exact `0.2.0` baseline fingerprint, clean installs, `0.2.0`→`0.3.0` upgrades, extras variants, command parity, workspace import, and removed-member assertions.
- [x] R2 Install the project, pytest, and workspace dependencies before the full Python matrix gate.
- [x] R3 Compare runtime and canonical test contents with only explicit portable-path normalization, not path basenames alone.
- [x] R4 Add a deep fresh-bootstrap gate covering doctor, Python tests, web typecheck/tests/build, workflow catalog, and forbidden-authority scan.
- [x] R5 Bound synchronization fingerprints and validation to declared reusable inputs and cover forbidden binaries, credentials, host paths, and customer outputs.
- [x] R6 Automate wheel, sdist, final-dist, installed-environment, compiled-workspace, and maintenance-entry-point scans.
- [x] R7 Verify every forbidden module and command in installed clean/upgrade environments and exercise workspace health plus the compiled HTML shell.
