# Functional Gap Goal

`/goal Карта разрывов` processes exactly one card or one active `MRQ-*`.
Compare the confirmed customer customization with the configured
`target_release` source, record exact source evidence and runtime-data gaps,
update the selected unit, rebuild derived views when changed, validate it, and
stop. This goal does not claim the ordinary research queue or start a worker
pool.
